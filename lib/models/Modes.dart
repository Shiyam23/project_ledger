enum ViewMode { List, Graph }

enum TimeMode { Individual, Days, Weeks, Months, Years, Overall }

enum SortMode {
  DateDesc,
  DateAsc,
  AmountDesc,
  AmountAsc,
  NameDesc,
  NameAsc,
  CategoryDesc,
  CategoryAsc
}
